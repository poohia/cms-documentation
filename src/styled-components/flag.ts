import { Flag } from "semantic-ui-react";
import styled from "styled-components";

const FlagMenuItem = styled(Flag)`
  cursor: pointer;
  position: relative;
  left: 8px;
`;

export { Flag, FlagMenuItem };
